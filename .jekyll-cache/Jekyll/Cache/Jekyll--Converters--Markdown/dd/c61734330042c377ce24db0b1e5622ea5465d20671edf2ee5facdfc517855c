I"c<hr />

<p>Using Tesseract OCR w/ from a .PDF file;</p>

<ol>
  <li>
    <p>Tesseract doesn’t read PDFs.</p>
  </li>
  <li>
    <p>However Imagemagik can convert PDF to .tiff.</p>
  </li>
  <li>
    <p>Warning: Tiff files can be very large and require lots of RAM.</p>
  </li>
</ol>

<p>First try an 72dpi TIFF, If this does not work then go to 300 dpi bit size.</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>convert file.pdf -depth 8 file.tiff

tesseract file.tiff output
</code></pre></div></div>

<ol>
  <li>300 dpi size - when using Tesseract the output is default for .txt suffix.</li>
</ol>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>convert -density 300 file.pdf -depth 8 file.tiff

tesseract file.tiff output
</code></pre></div></div>

:ET