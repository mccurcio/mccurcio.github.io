I"�<hr />

<p><a href="https://www.gnu.org/software/gawk/manual/gawk.pdf">gawk Documentation</a></p>

<h3 id="1-check-number-of-columns--f--specifies-field-separator-as-comma">1. Check number of columns: -F “,” specifies field separator as comma</h3>

<pre><code class="language-{bash}">awk -F "," '{print NF}' uniqMovie.csv | head -1
</code></pre>

<h3 id="2-filter-the-data-to-get-only-1st-2nd-6th-and-last-column">2. Filter the data to get only 1st, 2nd, 6th and last column</h3>

<pre><code class="language-{bash}">awk 'BEGIN{FS=",";OFS=",";}{print $1,$2,$6,$NF}' uniqMovie.csv &gt; newMovie.csv`
</code></pre>

<h3 id="3-use-awk-to-rearrange-columns">3. Use awk to rearrange columns</h3>

<pre><code class="language-{bash}">awk '{print $2 " " $1}' file.txt
</code></pre>

<h3 id="4-sum-column-one-from-a-file">4. Sum column one from a file</h3>

<pre><code class="language-{bash}">awk '{s+=$1} END {print s}' mydatafile
</code></pre>

<h3 id="5-one-of-my-awk-aliases-is-the-mean-and-sd-of-column-1">5. One of my awk aliases is the mean and sd of column 1</h3>

<pre><code class="language-{bash}">awk '{s+=$1;s2+=($1$1)} END {print s/NR,sqrt((NRs2-ss)/(NR(NR-1)))}'
</code></pre>

<h3 id="6-a-handy-alias-for-summing-up-a-column-of-numbers-from-the-command-line">6. A handy alias for summing up a column of numbers from the command line:</h3>

<pre><code class="language-{bash}">sumcol='awk '\''{ SUM += $1} END { print SUM}'\' 
</code></pre>
:ET