I"�<hr />

<p>sort</p>

<ol>
  <li>sort a space-delimited file based on its first column, then the second if the first is the same, and so on:</li>
</ol>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>sort input.txt
</code></pre></div></div>

<ol>
  <li>sort a huge file (GNU sort ONLY):</li>
</ol>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>sort -S 1500M -t $HOME/tmp input.txt &gt; sorted.txt
</code></pre></div></div>

<ol>
  <li>sort starting from the third column, skipping the first two columns:</li>
</ol>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>sort +2 input.txt
</code></pre></div></div>

<ol>
  <li>sort the second column as numbers, descending order; if identical, sort the 3rd as strings, ascending order:</li>
</ol>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>sort -k2,2nr -k3,3 input.txt
</code></pre></div></div>

<ol>
  <li>sort starting from the 4th character at column 2, as numbers:</li>
</ol>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>sort -k2.4n input.txt
</code></pre></div></div>

<ol>
  <li>Sort and remove duplicate lines in a file in one step without intermediary files</li>
</ol>

<p>Explanation</p>

<p>We open a file with vi and run two vi commands (specified with +):
%!sort | uniq
% = range definition, it means all the lines in the current buffer.
! = run filter for the range specified. Filter is an external program, in this example sort | uniq
wq = write contents to file and quit.
vi +’%!sort | uniq’ +wq file.txt</p>

:ET