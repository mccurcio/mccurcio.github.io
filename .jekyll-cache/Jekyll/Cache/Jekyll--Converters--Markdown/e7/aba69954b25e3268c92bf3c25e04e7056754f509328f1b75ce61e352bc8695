I"�<hr />

<p>Measure the execution time of small bits of Python code with the “timeit”</p>

<p>The “timeit” module lets you measure the execution time of small bits of Python code</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>&gt;&gt;&gt; import timeit
&gt;&gt;&gt; timeit.timeit('"-".join(str(n) for n in range(100))',
                  number=10000)
0.3412662749997253

&gt;&gt;&gt; timeit.timeit('"-".join([str(n) for n in range(100)])',
                  number=10000)
0.2996307989997149

&gt;&gt;&gt; timeit.timeit('"-".join(map(str, range(100)))',
                  number=10000)
0.24581470699922647
</code></pre></div></div>

<p><a href="">man page</a></p>
:ET